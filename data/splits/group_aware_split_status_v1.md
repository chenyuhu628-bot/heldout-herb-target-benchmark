# Group-aware Split Status v1

`GROUP_AWARE_SPLIT_FROZEN — GRAPH MATERIALIZATION AND PIPELINE SMOKE STILL REQUIRED`

One mapping was generated with the frozen seed and passed all hard invariants. The fixed test labels were historically sealed and are public in v1.2 for reproducibility. Model training and model selection must not use them. Only Stage C3 graph materialization, isolation tests, and non-formal pipeline smoke may follow.
