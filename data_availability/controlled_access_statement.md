# Controlled Access Statement

Materials that could expose protected evaluation labels, target-attributable held-out rows, prediction-level outputs, checkpoints, or restricted third-party raw records are outside the public repository.

The public package is intended for review-safe inspection of protocols, aggregate metrics, final figures, tables, source notes, aggregate figure source data, and public audit summaries.
