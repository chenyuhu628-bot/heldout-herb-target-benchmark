# Formal Checkpoint Contract v1

Formal checkpoints require protocol version, model, config ID/hash, run seed, input hashes, and code version. Missing metadata or config-hash mismatch rejects reload. C3 smoke checkpoint paths are rejected. D1 creates no retained checkpoint.
