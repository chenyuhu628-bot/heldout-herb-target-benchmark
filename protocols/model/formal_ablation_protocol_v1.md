# Formal Ablation Protocol v1

ABL01-ABL05 are preregistered exactly as the registry states. They use TCMRGAT's selected formal configuration and final-seed protocol. Removing herb-compound context is excluded from primary ablations because it destroys unseen-herb input; it may only be a labeled degenerate diagnostic. Test results cannot determine which ablations are retained. D1 registers but does not run them.
