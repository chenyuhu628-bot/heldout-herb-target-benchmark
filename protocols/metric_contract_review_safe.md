# Metric Contract

The primary sealed-test metric is macro herb-level AUPR, aggregated as the mean across five final seeds with sample standard deviation.

Secondary descriptive metrics include macro AUROC and recall at 50. Secondary metrics support interpretation but do not replace the primary ranking metric.

Reported sealed-test values are aggregate values only. No prediction matrix, score matrix, or target-attributable held-out row is included in this release.
