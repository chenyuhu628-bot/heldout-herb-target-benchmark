# File Inventory Audit

Status: PASS

Every file in the public repository is listed in `MANIFEST.csv`. Every file except `CHECKSUMS_SHA256.txt` is covered by `CHECKSUMS_SHA256.txt`.

The repository zip contains only the public repository folder.
